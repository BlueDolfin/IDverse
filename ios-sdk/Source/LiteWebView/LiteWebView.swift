/// LiteWebView — a reusable controlled-webview container.
/// Product-agnostic by design: no vendor domains, no transaction vocabulary.
public enum LiteWebViewInfo {
    public static let version = "0.1.0"
}
